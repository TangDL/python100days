import sys

def main():
    [n, d] = list(map(int, sys.stdin.readline().strip().split()))
    postions = list(map(int, sys.stdin.readline().strip().split()))
    scores = list(map(int, sys.stdin.readline().strip().split()))
    def backtrack(pos, res, ans):


    res = backtrack(0, 0, 0)
